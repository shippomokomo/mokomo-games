---
import BaseLayout from '../layouts/BaseLayout.astro';
import MediaLightbox from '../components/MediaLightbox.astro';
import { withBase } from '../utils/paths';
import { artists } from '../data/artists';

const references = {
  keyArt: {
    src: withBase('/images/references/key-art_2026.webp'),
    alt: 'Mokomo Key Art',
    artist: artists.kikuri,
  },

  current: {
    src: withBase('/images/references/reference-sheet_2025.webp'),
    alt: 'Mokomo Reference Sheet September 2025 to Current',
    artist: artists.kikuri,
  },

  past: [
    {
      period: '2021 to September 2025',
      src: withBase('/images/references/reference-sheet_2021.webp'),
      alt: 'Mokomo Reference Sheet 2021 to September 2025',
      artist: artists.minmi,
    },
    {
      period: '2022 to September 2025',
      src: withBase('/images/references/reference-sheet_2022.webp'),
      alt: 'Mokomo Reference Sheet 2022 to September 2025',
      artist: artists.minmi,
    },
  ],
};
---

<BaseLayout
  title="References | Mokomo Games"
  description="七宝もこものキャラクターデザインや三面図など、イラスト制作向けの参考資料を掲載しています。"
>
  <h1>References</h1>

  <div class="references-page">
    <div class="intro">
      <p>
        いつもイラスト等を作成してくださりありがとうございます！<br />
        こちらで三面図などの資料をご参照ください！
      </p>
    </div>

    <section class="reference-section">
      <h2>Key Art</h2>

      <div class="large-reference key-art">
        <div class="image-frame">
          <img
            class="reference-image"
            data-lightbox-trigger
            src={references.keyArt.src}
            data-full={references.keyArt.src}
            alt={references.keyArt.alt}
            loading="lazy"
            tabindex="0"
          />
        </div>

        <div class="artist-info">
          <span class="artist-credit-label">Design &amp; Illustration:</span>
          <a
            href={references.keyArt.artist.url}
            target="_blank"
            rel="noopener noreferrer"
          >
            {references.keyArt.artist.name}さん
          </a>
        </div>
      </div>
    </section>

    <section class="reference-section">
      <h2>Reference Sheet</h2>

      <div class="current-reference">
        <h3>September 2025 to Current</h3>

        <div class="large-reference">
          <div class="image-frame">
            <img
              class="reference-image"
              data-lightbox-trigger
              src={references.current.src}
              data-full={references.current.src}
              alt={references.current.alt}
              loading="lazy"
              tabindex="0"
            />
          </div>

          <div class="artist-info">
            <span class="artist-credit-label">Design &amp; Illustration:</span>
            <a
              href={references.current.artist.url}
              target="_blank"
              rel="noopener noreferrer"
            >
              {references.current.artist.name}さん
            </a>
          </div>
        </div>
      </div>

      <hr class="reference-divider" />

      <div class="past-reference-grid">
        {references.past.map((reference) => (
          <div class="past-reference">
            <h3>{reference.period}</h3>

            <div class="image-frame">
              <img
                class="reference-image"
                data-lightbox-trigger
                src={reference.src}
                data-full={reference.src}
                alt={reference.alt}
                loading="lazy"
                tabindex="0"
              />
            </div>

            <div class="artist-info">
              <span class="artist-credit-label">Design &amp; Illustration:</span>
              <a
                href={reference.artist.url}
                target="_blank"
                rel="noopener noreferrer"
              >
                {reference.artist.name}さん
              </a>
            </div>
          </div>
        ))}
      </div>
    </section>
  </div>

  <MediaLightbox id="lightbox" />

  <script>
    const images = Array.from(
      document.querySelectorAll<HTMLImageElement>(
        '.reference-image'
      )
    );

    const lightbox =
      document.querySelector<HTMLElement>('#lightbox');

    const lightboxImage =
      document.querySelector<HTMLImageElement>(
        '.lightbox-image'
      );

    const closeButton =
      document.querySelector<HTMLButtonElement>(
        '.lightbox-close'
      );

    const prevButton =
      document.querySelector<HTMLButtonElement>(
        '.lightbox-prev'
      );

    const nextButton =
      document.querySelector<HTMLButtonElement>(
        '.lightbox-next'
      );

    let currentIndex = 0;

    function showImage(index: number) {
      if (
        !lightbox ||
        !lightboxImage ||
        images.length === 0
      ) {
        return;
      }

      if (index < 0) {
        index = images.length - 1;
      }

      if (index >= images.length) {
        index = 0;
      }

      currentIndex = index;

      const image = images[currentIndex];
      const fullSrc = image.getAttribute('data-full');

      if (!fullSrc) return;

      lightboxImage.setAttribute('src', fullSrc);

      lightboxImage.setAttribute(
        'alt',
        image.getAttribute('alt') ?? ''
      );

      lightbox.classList.add('is-open');

      lightbox.setAttribute(
        'aria-hidden',
        'false'
      );

      document.body.classList.add(
        'lightbox-open'
      );
    }

    function openImage(
      image: HTMLImageElement
    ) {
      const index = images.indexOf(image);

      if (index === -1) return;

      showImage(index);
    }

    function closeLightbox() {
      if (!lightbox || !lightboxImage) return;

      lightbox.classList.remove('is-open');

      lightbox.setAttribute(
        'aria-hidden',
        'true'
      );

      lightboxImage.setAttribute('src', '');
      lightboxImage.setAttribute('alt', '');

      document.body.classList.remove(
        'lightbox-open'
      );
    }

    function showPrevious() {
      showImage(currentIndex - 1);
    }

    function showNext() {
      showImage(currentIndex + 1);
    }

    images.forEach((image) => {
      image.addEventListener('click', () => {
        openImage(image);
      });

      image.addEventListener(
        'keydown',
        (event) => {
          if (
            event.key === 'Enter' ||
            event.key === ' '
          ) {
            event.preventDefault();
            openImage(image);
          }
        }
      );
    });

    closeButton?.addEventListener(
      'click',
      closeLightbox
    );

    prevButton?.addEventListener(
      'click',
      showPrevious
    );

    nextButton?.addEventListener(
      'click',
      showNext
    );

    lightbox?.addEventListener(
      'click',
      (event) => {
        if (event.target === lightbox) {
          closeLightbox();
        }
      }
    );

    document.addEventListener(
      'keydown',
      (event) => {
        if (
          !lightbox?.classList.contains(
            'is-open'
          )
        ) {
          return;
        }

        if (event.key === 'Escape') {
          closeLightbox();
        }

        if (event.key === 'ArrowLeft') {
          showPrevious();
        }

        if (event.key === 'ArrowRight') {
          showNext();
        }
      }
    );
  </script>
</BaseLayout>

<style>
  /*
   * References全体
   */
  .references-page {
    max-width: 1400px;
    margin: 0 auto;
  }

  /*
   * ページ冒頭
   */
  .intro {
    margin-bottom: 3.5rem;
  }

  .intro p {
    margin: 0;

    color: var(--color-text-soft);

    line-height: 1.9;
  }

  /*
   * 各セクション
   */
  .reference-section {
    margin-bottom: 5rem;
  }

  .reference-section h2 {
    margin: 0 0 1.75rem;

    color: var(--color-text);

    font-size: 2rem;
  }

  .reference-section h3 {
    margin-top: 0;
    margin-bottom: 1rem;

    color: var(--color-green-dark);

    font-size: 1.1rem;
    font-weight: 700;
  }

  /*
   * 大きなReference
   */
  .large-reference {
    max-width: 1100px;
    margin: 0 auto;
  }

  /*
   * 画像の白いフレーム
   *
   * 暖色背景の上で画像を
   * 少しだけ浮かせる
   */
  .image-frame {
    width: fit-content;
    max-width: 100%;
    margin: 0 auto;

    border-radius: 12px;

    background: #fff;

    overflow: hidden;
  }

  /*
   * 画像
   */
  .reference-image {
    display: block;

    width: 100%;
    height: auto;

    cursor: zoom-in;

    transition:
      transform 0.15s ease,
      opacity 0.15s ease;
  }

  .reference-image:hover {
    transform: scale(1.005);
    opacity: 0.96;
  }

  /*
   * Key ArtだけPCでは
   * 画面の高さに合わせて小さく表示
   */
  .key-art .reference-image {
    width: auto;
    max-width: 100%;
    max-height: 70vh;
    margin: 0 auto;

    object-fit: contain;
  }

  /*
   * 作者
   */
  .artist-info {
    display: flex;
    justify-content: center;
    align-items: baseline;
    gap: 0.35rem;
    flex-wrap: wrap;

    margin-top: 0.85rem;
  }

  .artist-credit-label {
    color: var(--color-text-soft);
  }

  .artist-info a {
    color: var(--color-green-dark);

    font-weight: 600;

    text-decoration-color:
      color-mix(
        in srgb,
        var(--color-green) 60%,
        transparent
      );

    text-underline-offset: 0.2em;

    transition:
      color 0.15s ease,
      text-decoration-color 0.15s ease;
  }

  .artist-info a:hover {
    color: var(--color-green);

    text-decoration-color:
      var(--color-green);
  }

  /*
   * 現在のReference Sheet
   */
  .current-reference {
    margin-bottom: 3rem;
  }

  .current-reference > h3 {
    text-align: center;
  }

  /*
   * 新旧Reference Sheetの区切り
   */
  .reference-divider {
    margin: 3.5rem 0;

    border: 0;
    border-top:
      1px solid var(--color-border);
  }

  /*
   * 過去のReference Sheet
   */
  .past-reference-grid {
    display: grid;
    grid-template-columns:
      repeat(2, minmax(0, 1fr));

    gap: 2rem;

    align-items: start;
  }

  .past-reference h3 {
    text-align: center;
  }


  /*
   * スマートフォン
   */
  @media (max-width: 700px) {
    .intro {
      margin-bottom: 2.5rem;
    }

    .reference-section {
      margin-bottom: 4rem;
    }

    .reference-section h2 {
      font-size: 1.7rem;
    }

    .large-reference {
      max-width: none;
    }

    /*
     * スマホではKey Artも
     * 横幅いっぱいを使う
     */
    .key-art .reference-image {
      width: 100%;
      max-width: none;
      max-height: none;
    }

    .past-reference-grid {
      grid-template-columns: 1fr;

      gap: 2.5rem;
    }

    .reference-divider {
      margin: 2.5rem 0;
    }

    .image-frame {
      border-radius: 10px;
    }

  }
</style>
